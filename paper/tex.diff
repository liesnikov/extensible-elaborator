diff --git a/main.tex b/main.tex
index 9ac3b1b..2dd8924 100644
--- a/main.tex
+++ b/paper.tex
@@ -1,3 +1,8 @@
+% this file is (almost) automatically generated by pandoc
+% the only thing changed from the output is the addition of
+% \bibliographystyle{ACM-Reference-Format} at the bottom
+% and removal of the default bibliography style at the top
+
 % Options for packages loaded elsewhere
 \PassOptionsToPackage{unicode}{hyperref}
 \PassOptionsToPackage{hyphens}{url}
@@ -130,7 +135,6 @@ This allows for a more compact base elaborator implementation while enabling ext
   \usepackage{selnolig}  % disable illegal ligatures
 \fi
 \usepackage[]{natbib}
-\bibliographystyle{plainnat}
 \IfFileExists{bookmark.sty}{\usepackage{bookmark}}{\usepackage{hyperref}}
 \IfFileExists{xurl.sty}{\usepackage{xurl}}{} % add URL line breaks if available
 \urlstyle{same} % disable monospaced font for URLs
@@ -1570,6 +1574,7 @@ We see three main prospects for future work:
 \begin{CSLReferences}{0}{0}
 \end{CSLReferences}
 
+  \bibliographystyle{ACM-Reference-Format}
   \bibliography{bib.bib}
 
 \end{document}
