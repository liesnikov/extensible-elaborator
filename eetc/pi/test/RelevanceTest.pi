module RelevanceTest where

id : [x:Type] -> (y : x) -> x
id = \[x] y. (y : x)

-- as of 5591738 doesn't work because we assume relevance when checking App we assume
-- that arguments to the id here are relevant
-- ./extended-elab/eetc/src/TypeCheck/Elaborator.hs:95-97
t0 = id [Bool] True

-- same issue here
t1 = id [Bool] (id [Bool] True)